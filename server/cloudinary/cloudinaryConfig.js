const cloudinary = require("cloudinary").v2;
require("dotenv").config();

module.exports = cloudinary;

//

// const cloudinary = require("cloudinary").v2;  -- Cloudinary-Bibliothek importieren:
// require("dotenv").config(); -- dotenv importieren, um .env-Datei zu lesen:
// module.exports = cloudinary; -- Exportiere cloudinary, um es in anderen Dateien zu verwenden:
